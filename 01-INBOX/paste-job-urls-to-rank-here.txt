# Paste the job-posting URLs you want to rank here — one per line.
#
# - Lines starting with "#" are ignored (notes/comments are fine).
# - Even links you've only glanced at are fine; ranking is what tells you which are worth your time.
# - Then tell Claude in chat, e.g. "rank today's batch" — or run 03-VETTING/new_batch.py.
# - This file is a shared seed; your pasted URLs are snapshotted into each dated batch under
#   "__READY TO REVIEW/<date>/3 - Source Material/" when a batch is created.
