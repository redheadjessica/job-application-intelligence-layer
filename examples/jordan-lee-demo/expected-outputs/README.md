# expected-outputs/ — committed synthetic OUTPUTS

> 100% fictional. See [`../README.md`](../README.md) for the full safety rules.

This folder holds the **synthetic outputs** the workflow produces from the fixtures — the public-safe demo artifacts. Added in **V2.1 Unit 3+**.

## Planned contents

- **`rankings.csv`** — the ranked job table (durable source; the `.xlsx` renders from this).
- **`rankings.xlsx`** — the colored ranking spreadsheet. **Approved committed artifact** (small, inspectable). Renders from the CSV via `03-VETTING/make_rankings_xlsx.py --config <example jail.config.json>`.
- **One tailored markdown** — `application_resume_output - <Company> - <Role>.md`, including its "Questions for the candidate" section.
- **One `archive-summary.md`** — the archived-application record from `/archive`.
- **One reconcile sample** — a reconcile report, if produced.
- **`prep-report.md` sample** — a prep report showing usable vs thin / failed / duplicate.

## Honesty about provenance

Each artifact should note whether it was **actually generated by the workflow** or **hand-curated** to match the real output shape. Keep committed outputs minimal and regeneratable to limit drift from real engine behavior.
