# TEMPLATE (tracked, in ENGINE__PUBLIC_GIT_TRACKED/01-INBOX/). Your private working copy is
# "paste-job-urls-to-rank-here.txt" under PRIVATE__YOUR_FILES_GITIGNORED/01-INBOX__YOUR_PRIVATE_INFO/
# (gitignored) — ENGINE__PUBLIC_GIT_TRACKED/03-VETTING/new_batch.py creates it from this template on first run, or just ask
# Claude. Your pasted URLs are private data and must never be committed; the gitignore handles it.
#
# Paste the job-posting URLs you want to rank into the working copy — one per line.
# - Lines starting with "#" are ignored (notes/comments are fine).
# - Even links you've only glanced at are fine; ranking is what tells you which are worth your time.
# - Add as many as you've got in one sitting rather than trickling in one at a time — vetting scores
#   and fetches jobs in parallel, and ranking is most useful (and most useful relative to each other)
#   when it's comparing a real batch of options side by side, not judging one job in isolation.
# - Then tell Claude in chat, e.g. "rank today's batch" — or run ENGINE__PUBLIC_GIT_TRACKED/03-VETTING/new_batch.py.
# - The working copy is a shared seed; your pasted URLs are snapshotted into each dated batch under
#   "__READY_TO_REVIEW__PRIVATE_GITIGNORED/<date>/3 - Source Material/" when a batch is created.
