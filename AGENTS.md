# Agent instructions

This repo's coding-agent instructions live in `CLAUDE.md` at the repo root — read it
first. This file exists so Codex and any other tool that looks for `AGENTS.md` finds
the same rules (notably: **`docs/changelog.md` capture is required, not optional** —
see `CLAUDE.md` → "Repo changelog").
