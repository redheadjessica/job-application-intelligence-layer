# macOS
.DS_Store

# Python
.venv/
venv/
env/
__pycache__/
*.py[cod]

# Node
node_modules/

# Batch run outputs (keep the empty review folder, ignore generated batches)
__READY TO REVIEW/*/

# Logs / temp
*.log

# Intake — a user's own materials + generated assessment (personal, never commit)
intake/your-materials/*
!intake/your-materials/.gitkeep
intake/resume-assessment.md

# Machine-specific local Claude Code settings (regenerated; never commit)
.claude/settings.local.json
