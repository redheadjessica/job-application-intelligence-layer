# Python dependencies for the job-pipeline.
# Install into the repo virtualenv:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Sourcing / URL fetch (02-PREP/prep_job_urls.py):
#   .venv/bin/python3 02-PREP/prep_job_urls.py "<source-folder>" --input "<urls.txt>"
requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.8
# trafilatura -> justext imports lxml.html.clean, which newer lxml split out:
lxml_html_clean>=0.1

# Vetting: builds the formatted, conditionally-formatted rankings .xlsx (03-VETTING/make_rankings_xlsx.py):
openpyxl>=3.1

# Cover letters (optional module): builds the .docx paste source (04-TAILOR/cover-letter/make_cover_letter_docx.py):
python-docx>=1.1

# Reconcile: one-time PDF extraction of submitted applications (04-TAILOR/learning/extract_submission.py):
pypdf>=4.0

# The Playwright-based fallback fetcher (02-PREP/prep_job_urls_playwright.py) is
# optional and NOT installed by this file. It needs a browser download:
#   .venv/bin/pip install playwright && .venv/bin/playwright install chromium
