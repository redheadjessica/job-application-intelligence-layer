# Python dependencies for the job-pipeline.
# Install into the repo virtualenv:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Sourcing / URL fetch (prep/prep_job_urls.py):
#   .venv/bin/python3 prep/prep_job_urls.py "<source-folder>" --input "<urls.txt>"
requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.8
# trafilatura -> justext imports lxml.html.clean, which newer lxml split out:
lxml_html_clean>=0.1

# Vetting: builds the formatted, conditionally-formatted rankings .xlsx (vetting/make_rankings_xlsx.py):
openpyxl>=3.1

# The Playwright-based fallback fetcher (prep/prep_job_urls_playwright.py) is
# optional and NOT installed by this file. It needs a browser download:
#   .venv/bin/pip install playwright && .venv/bin/playwright install chromium
