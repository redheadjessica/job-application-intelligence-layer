# Summary Library — Learning Corpus

<!--
  This file starts EMPTY and fills up over time. It is not authored by intake — it accumulates the
  professional-summary lines you actually finalized and submitted, captured after the fact via the
  reconcile (Learn) loop. Those submitted summaries are voice ground-truth: the most reliable record
  of how you really write about yourself, and the corpus the tailor agent learns from when proposing
  future options.

  RELATIONSHIP TO 05-summary-quick.md
  - `05-summary-quick.md` is the quick menu the agent reads at generation time (a few reusable
    families + closers, filled by intake).
  - THIS file is the slower-moving evidence base behind it. As entries accumulate, recurring shapes
    here are what justify promoting a new family — or refining a stem — in the quick file.

  HOW ENTRIES GET ADDED (post-submission only — never during a generation run)
  - After you submit an application and the reconcile/Learn step runs, append the summary you
    actually shipped as a new row, using the shape below. Add entries; don't rewrite old ones.
  - This is learning data, not a menu to copy verbatim. The agent should adapt from it, not paste it.

  WHAT TO LOOK FOR AS ENTRIES GROW
  - The recurring SHAPE of your summaries (sentence count, where the proof point lands, how closers
    are phrased) — that shape is the reusable signal, more than any single line.
  - Which framings you reach for by domain, so the quick file's closers stay true to your voice.
-->

## How to record an entry

Append one row per finalized, submitted summary:

- **Application** — company + role (and date), and optionally a short archetype tag for the role type.
- **Summary** — the exact summary text you submitted.
- **Notes** — anything worth remembering: which family/closer it adapted from, a phrasing you'd reuse, domain-specific wording, or why you deviated from the menu.

| Application | Summary | Notes |
|-------------|---------|-------|
| <!-- e.g. Acme — Senior PM (consumer / retention), MM-DD-YY --> | <!-- exact submitted summary text --> | <!-- adapted from Family A · retention closer; reused "…" phrasing --> |

<!-- Append new finalized summaries above as the reconcile (Learn) step processes more submitted
     applications. The table grows downward over time; keep the oldest examples — they show how your
     voice has held or shifted. -->
